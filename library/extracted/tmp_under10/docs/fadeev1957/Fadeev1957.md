## У С Х Е Х И М А Т Е М А Т И Ч Е С К И Х Н А У К

## к п о н т я т н о й э н т р о п и н о к о н е ч н о й в е р е я т н о т н о й с х е м е ы

## Д. К. Фаддеев

- 1°. Как установлено в статье А. Я. Хинчина [1], энтропия Н (p1, p2, …, p) конечной вероятностной схемы (A1, A2, …, A) может быть охарактеризована следующей системой аксиом.
- 1. Н (p1, p2, …, p) непрерывна относительно p1, p2, …, pн в области 0
- 2. Н (p1, p2, …, p) симметрична относительно p1, p2, …, pн.
- 3. Н (p1, p2, …, pн, 0) = Н (p1, p2, …, pн).
- 4. Н (q11, …, qн; 2р, …, q2н; …; qн1, …, qн) = Н (p1, p2, …, pн)+

$$+ \sum _ { i = 1 } ^ { n } p _ { i } H \left ( \frac { q _ { i } } { p _ { i } } , \dots , \frac { q _ { i n } } { p _ { i } } \right ) ,$$

где

$$p _ { i } = q _ { i 1 } + \dots + q _ { i m } \quad ( i = 1 , \dots , n )$$

(эта аксиома сформулирована в статье [1] в других терминах).

$$5 . \, H ( p _ { 1 } , p _ { 2 } , \dots , p _ { n } ) \leq H \left ( \frac { 1 } { n } , \frac { 1 } { n } , \dots , \frac { 1 } { n } \right ) .$$

Этими аксиомами энтропия определяется однозначно с точностью до подолжительного множителя. Целью настоящей заметки является некоторое упрощене системы аксиом. Именно, предлагаются следующие аксномы: 1' Н (p1, 1-р) непрерывна при 0

- 2' Н (p1, p2, …, pн) симметрична относительно p1, p2, …, pн.
- 3' При n &gt; 2

$$H ( p _ { 1 } , \dots , p _ { n - 1 } ; q _ { 1 } , q _ { 2 } ) = H ( p _ { 1 } , \dots , p _ { n - 1 } , p _ { n } ) + p _ { n } H \left ( \frac { q _ { 1 } } { p _ { n } } , \frac { q _ { 2 } } { p _ { n } } \right ) .$$

Здесь pн=q1+q2.

Разница в этих двух системах аксиом заключается в том, что, во-первых, аксиома 5 (экстремальность) заменяется требованием положительности энтропии в одной точке и, во-вторых, аксиомы 3 и 4 заменяются одной аксиомой 3', очень естественной, если рассматривать энтропию как меру неопределённости схемы. Действительно, «неопределённость» схемы

Доказательство. Нужно n раз применить лемму 5 к каждой группе аргументов, что возможно в силу снимметрии.

Положив m1=m2=...=m n=m, получим, что из 2', 3' следует 4. Итак, мы установилы равносильность групп аксиом 2,3, 4 и 2', 3' 3°. Положим, следующая статье [1], F(n)=H 1 n , 1 n ,..., 1 n

F (1)=0. Применял лемму 6 к случаю m1=...=m n=m, qij=1 mn, получим:

$$F ( m n ) = F ( m ) + F ( n )$$

при m&gt;2, n&gt;2. При n=1 или n=1 соотношение (1) тривнально. Далее, применялся лемму 5 к H (1; 1;..., 1 n ), получим:

$$H \left ( \frac { 1 } { n } , \frac { 1 } { n } , \dots , \frac { 1 } { n } \right ) = H \left ( \frac { 1 } { n } , \frac { 1 } { n } \right ) + \frac { n - 1 } { n } H \left ( \frac { 1 } { n - 1 } , \dots , \frac { 1 } { n - 1 } \right ) ,$$

откуда следует:

$$\eta _ { n } = H \left ( \frac { 1 } { n } , \frac { n - 1 } { n } \right ) = F ( n ) - \frac { n - 1 } { n } F ( n - 1 ) .$$

Лемма 7. При n=0

$$n _ { n } = \frac { F ( n ) } { n } \rightarrow 0 \ \ n \ \lambda _ { n } = F ( n ) - F ( n - 1 ) \rightarrow 0 .$$

Доказательство. В силу непрерывности H(p,1-p) при p= 1n=H 1 n , 1 n , 1 n )→H (0, 1)=0. Далее, n,n=nF(n) (n-1) F(n-1), откуда

$$n F ( n ) = \sum _ { k = 1 } ^ { n } k \tau _ { k }$$

и

$$\frac { F ( n ) } { n } = \frac { 1 } { n ^ { 2 } } \sum _ { k = 1 } ^ { n } k \eta _ { k } = \frac { n + 1 } { 2 n } \cdot \frac { 2 } { n ( n + 1 ) } \sum _ { k = 1 } ^ { n } k \eta _ { k } .$$

При переходе к пределу при 1 2 0 0 0 0 0 получим:

$$( c _ { p } - c _ { 2 } ) \log 2$$

что невозможно. Таким образом, для всех p' имеет место cp'  c2 при всех p'. Лемма доказана.

Теорема.

$$H \left ( p _ { 1 } , p _ { 2 } , \dots , p _ { n } \right ) = c \sum _ { i = 1 } ^ { n } p _ { i } \log \frac { 1 } { p _ { i } } \quad ( c > 0 ) .$$

Доказательство. Рассмотрим случай л = 2. Пусть p = r при целых r и s.